function reverseDigitsInPairs(n) {
    
    let numbers = [n]
    console.log(n)
    const last_num = (n.length - 1)
    
    for(let i = 0; i < n.length; i++){
        const num_pairs = n[0][1];
        return num_pairs
    }
}

console.log(reverseDigitsInPairs(123456))